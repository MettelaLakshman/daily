class Solution {
public:
    int minimumCost(vector<int>& nums) {
        int s=0;
        s+=nums[0];
        sort(nums.begin()+1,nums.end());
        s+=nums[1];
        s+=nums[2];
        return s;
    }
};
